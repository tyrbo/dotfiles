- clean: ['~']

- shell:
  - mkdir -p ~/.config/nvim

- link:
    ~/.hyper.js: hyper.js
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.config/nvim/init.vim: nvim.vim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
