- clean: ['~']

- link:
  ~/.dotfiles: ''
  ~/.gitconfig: gitconfig
  ~/.inputrc: inputrc
  ~/.tmux.conf: tmux.conf
  ~/.vim: vim/
  ~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
