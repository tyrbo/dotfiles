- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.emacs.d: emacs.d

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [./base16-railscasts.dark.sh]
